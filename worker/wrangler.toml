name = "recipe-glass-api"
main = "src/index.ts"
compatibility_date = "2026-09-01"

# 非機密設定放這裡；AI_API_KEY 與 APP_TOKEN 一律用
# `wrangler secret put` 設定，不要寫進這個檔案（它會進版控）。
[vars]
AI_BASE_URL = "https://api.openai.com/v1"
AI_MODEL = "gpt-4o-mini"
ALLOW_ORIGIN = "*"
